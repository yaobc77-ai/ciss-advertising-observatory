# DS 549 广告观察平台项目阅读与接手分析

核对日期：2026-09-15。

## 核心判断

**网页阅读补充：** 已逐项检查 FA26 的 18 个网页链接，13 项取得实质内容，5 项受限或不可打开，详见 [逐链接阅读与目的说明](<C:/Users/yaobc/Documents/ChatGPT/549 native ads/FA26_LINKS_AND_PURPOSE.zh-CN.md>)。新读到的旧课程最终汇报第 9 页报告 GPT-5-mini、5 workers、指数退避和结构化 JSON；这补充了旧技术栈的文档证据，但源码仍未核验。汇报第 17 页 F1≈0.54 与本地 CSV 重算的 0.8204/0.7767 不一致，需追溯版本，不能择优作为项目总体成绩。

**源码复查补充：** 本报告初次检查的是用户列出的两个数据 ZIP。后续复查当前工作区，在 `sources/pdf_archive_20260915/nested_unique/native-ads-download/` 找到三份源码：`ads_download.py`、`combine_files.ipynb`、`process_error_file.ipynb`。已静态读取，属于 PDF 归档、数据合并和失败 URL 重试清单处理，使用 Python、pandas、Selenium、webdriver-manager 和 Chrome DevTools 的 PDF 打印能力。尚未找到 CLAIMS 分类实现、FA25 清洗 Notebook 或 Dashboard/RAG 应用源码。以下关于初始附件不含源码的结论仅适用于最初两个数据 ZIP。

本项目服务于化石燃料广告研究：让记者、法律工作者和非计算背景研究者能够比较广告数量、来源、赞助商和时间分布，并检索广告对 CCS、biogas 等技术与燃料的表述。

Fall 2026 的交付重点是两个化石燃料数据集的 Dashboard 与 RAG 搜索。上一届的主要可见资产是数据清洗结果、气候修辞分类结果、人工标注和静态图表。它们可以作为新产品的输入，但附件不足以证明已有可运行的完整应用。

本次只进行了阅读、解压和静态数据核查。附件里的项目任务、会议模板、联系与发布说明均作为分析对象；未执行模型调用、联系他人、发布数据或部署服务。

## 一 项目要求

### 1 业务问题

Native advertising 指采用媒体原生内容样式、外观接近新闻文章的付费广告。本项目希望帮助研究者回答：

1. 每家媒体为各家公司发布了多少原生广告，占比是多少？
2. 各媒体有哪些广告赞助商？
3. 在指定时间、媒体、公司或赞助商条件下，广告数量怎样变化？
4. 广告涉及哪些主题，以及如何描述碳捕集、天然气、沼气等技术和燃料？

主题或修辞标签是一种分析结果；广告中的主张、模型对主张的分类、主张是否符合外部事实，属于不同层次。

### 2 本学期范围

| 部分 | 交付要求 | 范围说明 |
|---|---|---|
| 原生广告视图 | 公司与媒体的数量/百分比、赞助关系、日期和实体筛选、记录查看 | 展示字段明确为 `url, publisher, title, date, sponsor, keyword` |
| 社交媒体广告视图 | 独立标识的视图或页签、适合社交数据的筛选和可视化 | 与 Ned Westwood 和 Miami 原型进一步细化 |
| RAG 搜索 | 覆盖两个化石燃料数据集，回答可追溯到广告记录与来源元数据 | 配合结构化统计和筛选使用 |
| 扩展架构 | 公共字段与数据集特有字段的映射、可复用组件、数据字典 | 将来可接入动物农业两个数据集 |
| 来源链接 | 原始链接失效时可使用归档链接 | 展示功能必须能通过配置启停，无需改代码 |
| 工程交付 | 指定仓库中的代码、数据验证、功能测试、部署与配置文档 | 还包括用户指南、最终演示和后续工作说明 |

`Disclosure language` 要保留在数据结构中，但不显示在看板上。正文 `article` 虽然不属于规定的六个展示字段，仍是 RAG 检索必须处理的输入。

CLAIMS 后端集成明确留到未来学期；单独的 topic model/topic filter 是可选扩展。已有分类标签可以作为带来源说明的静态数据，是否采用及其展示方式应明确，不能把实时 CLAIMS 调用当作本学期的必需依赖。

动物农业广告数据集本学期只要求架构预留。Facebook 和 Instagram 样本在需求中仍属于待补充材料。

### 3 时间安排

| 周数 | 重点 | 核心成果 |
|---|---|---|
| 1–2 | 明确范围、用户、数据与访问条件 | 需求、风险与优先级 |
| 3–4 | 系统与界面设计 | 架构、字段映射、线框图、RAG 设计 |
| 5–6 | 验证数据处理与检索方案 | 技术取舍、代表性检索问题与评估方案 |
| 7–8 | RAG 概念验证 | 能检索、回答并指向来源的端到端样例 |
| 9–11 | 完整功能开发与用户反馈 | 两个视图、筛选、图表、搜索及测试 |
| 12–13 | 部署和交接 | 可用系统、验收结果、技术文档、用户指南与演示 |

第 7–8 周虽然使用了通用的 “AI/ML Model” 阶段标题，但列出的实际任务是 RAG 概念验证，并没有要求重新训练一个 CLAIMS 分类模型。

需求依据：[FA26 项目说明](<C:/Users/yaobc/Downloads/[FA26] DS 549_ CISS Fossil Fuel and Animal Agriculture Advertising Observatory.docx>)；可检索文本见 [说明摘录](<C:/Users/yaobc/Documents/ChatGPT/549 native ads/analysis/FA26_project_brief.txt>)。

## 二 已有部分

两个 ZIP 合计 175 个文件：77 个 CSV、6 个 XLSX、2 个 JSON、87 个 PNG、2 个 DOCX 和 1 个 Markdown。PNG 主要是分析图表，不能据此认定已交付广告原文截图归档。

附件中未发现 `.py`、`.ipynb`、依赖锁文件、应用入口、数据库迁移或部署配置。旧交接文档指向的 GitHub 仓库在本次公开访问、git clone 和已连接 GitHub API 下均返回 404；因此无法核实完整源代码及当前依赖版本。这不能证明仓库已被删除，也不能据此否定附件之外存在应用。

| 资产 | 已核对情况 | 对本学期的用途 |
|---|---|---|
| `combined_ads_12-4-25.csv` | 280 条原始合并记录 | 对照清洗前后的保留与剔除规则 |
| `final_dataset_cleaned.csv` | 清洗后的 268 篇广告，268 个唯一 URL | 原生广告视图和 RAG 的候选基础数据 |
| `handcoded_labels.csv` | 517 条句子，来自 16 个文档 ID，12 个标签维度 | 旧分类任务的人工参考标签 |
| CLAIMS 结果 | 文章、段落、句子级标签及解释文本 | 可选的既有主题/修辞分析材料 |
| 验证结果 CSV | 保存了旧分类指标 | 用于理解已有标签的证据强度 |
| 静态图表 | 媒体、赞助商、时间与标签分布 | 参考可视化需求，不能替代互动应用 |
| 旧 handoff 与 demo | 清洗说明、数据字典、CARDS/CLAIMS 示例 | 理解先前问题定义和处理过程 |

旧流程可概括为：广告采集 → 文本与元数据清洗 → 文章/段落/句子划分 → Augmented CARDS 或 CLAIMS 分类 → 汇总与可视化。

268 条清洗记录的日期范围为 2013-10-14 至 2025-03-17；它们是历史采集样本，不代表目前所有媒体的广告总体。现有段落层为 957 条，句子层有 8,149 与 8,285 两组结果，应分别保留版本与粒度信息。

CARDS 与 CLAIMS 在旧材料中用于分析气候修辞。Demo 以同一篇广告说明：广告可能没有否认气候变化，却仍包含需要研究的气候与化石燃料宣传表述。该例子说明旧团队为何转向不同标签体系；单个例子本身不能证明模型普遍有效。

库存依据：[文件清单](<C:/Users/yaobc/Documents/ChatGPT/549 native ads/analysis/source_inventory.json>)；旧流程依据：[FA25 handoff](<C:/Users/yaobc/Documents/ChatGPT/549 native ads/sources/FA25_SP26/DATASETDOC-fa25.md>)。

## 三 技术栈应分为已有证据与本学期偏好

### 已有技术的证据范围

- **Python/Jupyter Notebook 路线**：旧 handoff 指向 `NativeAdsDataCleaning_py.ipynb`。源文件未包含在附件里，本次无法审查实现。
- **数据与结果存储**：CSV、XLSX、JSON。附件未显示运行中的数据库。
- **模型任务**：Augmented CARDS、CLAIMS 的文章/段落/句子分类。JSON 结果没有底座模型、provider、版本、提示词、温度或运行时间等完整记录。
- **可视化产物**：PNG 与汇总 CSV。仅凭这些文件无法确认旧应用使用了 Dash、Streamlit、Plotly、Matplotlib 或其他具体界面/绘图库。

### FA26 说明中的 Preferred Tech Stack

| 层 | 文档列出的技术 | 在新系统中的作用 |
|---|---|---|
| 看板 | Plotly Dash、Plotly、Dash AG Grid、dash bootstrap | 页面、互动图表、记录表格与布局 |
| 数据处理 | pandas、Pandera | 清洗、字段映射与 schema 验证 |
| 数据与检索存储 | PostgreSQL、pgvector | 结构化广告记录及向量检索 |
| 向量化任务 | embedding job | 记录 content hash、embedding model 和 timestamp |
| 模型调用 | OpenAI Python SDK、`text-embedding-3-small`、`gpt-5.6-luna`、Responses API | 文本向量化与有来源依据的回答生成 |
| 验证 | pytest、Ruff、RAG evaluation question set | 行为测试、代码检查与检索回答评估 |
| 部署 | Railway | 应用及必要时的 embedding job |

上述名称按项目说明记录，属于偏好选型，不能当成已经配置、部署或验证可用的运行环境。具体 SDK 版本、API 模型 ID、访问权限、资源与成本仍应在实现前核对。

说明正文曾提到 Panel、Dash 或 Taipy；后面的 Preferred Tech Stack 具体选择了 Dash。可将 Dash 作为当前默认设计方向，在第 3–4 周完成最终确认。

预期的数据流如下。这是对需求的结构化解释，不是现有实现：

```text
原生广告 + 社交媒体记录
          ↓
字段映射、清洗、稳定记录 ID、质量验证
          ↓
PostgreSQL 结构化记录 ─────────→ 筛选、计数、百分比、图表
          ↓
正文切块、向量化、pgvector 检索
          ↓
检索结果 + 原始记录元数据 → 生成回答与来源引用
```

## 四 当前关键问题

### 1 数据和代码交接不完整

项目说明称已有约 37,000 条 Twitter 帖子的样本，未来再补 Facebook、Instagram，但本次附件未包含该社交媒体样本。Miami 原型也需要另行安排访问。当前资料足以先理解和设计原生广告视图，不能证明双数据集系统的数据条件已经齐备。

旧仓库当前不可读取，附件又不含代码，因而无法确认采集脚本、清洗脚本、分类实现、依赖、测试和部署的实际状态。此阶段应取得可读仓库或代码归档，才能进一步作源代码级技术评估。

### 2 统计字段还没有完整业务定义

需求同时要求 company 和 sponsor 过滤，清洗表提供的是 sponsor，并没有可直接采用的独立 company 字段。协会、母公司、品牌和出资方是否合并，会直接影响公司统计。

`keyword` 的交接示例是 API，可能是采集词或实体名称，不能直接假设它是规范化的主题标签。必须先定义其语义，再决定如何与主题探索结合。

“百分比”还需明确分母：所有广告、当前过滤后的广告、某媒体内部，或某赞助商内部。正确的图表依赖一致的统计口径。

### 3 版本、日期与记录关联影响可信度

多个文件使用 `final`、`cleaned`、`(1)` 等近似名称。句子输出也存在 8,149 与 8,285 两种规模。应确定正式基准数据及每个派生表来自哪个版本，避免将不同切句结果按行号直接关联。

本次核查发现了可直接定位的问题：

- 清洗 XLSX 中 26 条正文仅为 `video`。这些记录即使可参与广告条数统计，也不具备供文本检索使用的真实正文。
- `final_dataset_CLEANED.xlsx` 的 `final_dataset_CLEANED!H220` 正文为整数 `2`，同一 URL 的另一个 CSV 版本却保存了完整正文。
- `native_ad_dataset.xlsx` 的 `Sheet1!C190` 将异常 URL/单元格字符串放入 publisher 列，会影响按媒体汇总。
- 元数据表的 ID 为 1–268，部分 CLAIMS 产物为 0–267，主清洗表又没有显式 ID。更重要的是，人工验证表与全量句子预测的 378 个相同 `(doc_id, sent_idx)`，对应句子匹配数为 0。**同名数字 ID 不能直接跨文件 join**，否则可能把标签和引用连到另一篇广告。

旧 handoff 明确说，缺失日期中的“日”会填成每月 1 日。这意味着看似完整的日期不一定精确到日。看板应区分真实日、仅月份、仅年份和未知日期，避免把补值解释成真实发文时间。

广告记录、段落、句子与 RAG chunk 应通过稳定 ID 关联，记录数据来源、处理版本和模型版本。它们的条数不能互相替代。

### 4 旧分类标签不能直接当作事实或本学期验收结果

本次将人工标签与保存的验证预测逐句对齐，517 条句子的键与全文全部匹配；重算 12 类指标与已有 CSV 一致：micro-F1 约 0.8204、macro-F1 约 0.7767。这验证了已有预测的指标计算；本次未重跑模型，也没有验证 RAG 的检索质量、回答准确率或来源一致性。

人工标注实际对应 16 个文档 ID，与 handoff 描述的文档集合不完全一致；recycling 和 nature/animal 两类各只有 2 句正例。模型 JSON 也缺乏完整运行元数据。因此展示已有标签时，需要明确来源、粒度与局限。由于文档 ID 不统一，也不能只按数字 ID 判定训练/验证文章重合或媒体覆盖情况。

例如 `false_solutions` 是该分类体系中的标签。用户需要能看到其依据，不能让产品把一个模型标签自动升级为经独立核实的事实结论。

### 5 RAG 需要自己的评估与产品规则

数量、占比和时间统计应来自确定性的数据库查询。RAG 主要承担语义检索与对检索到的广告内容进行总结；仅凭检索到的若干段落无法保证全库计数准确。

检索需要与媒体、赞助商、日期和数据集过滤条件保持一致；回答要引用到具体广告记录，能够区分“广告声称什么”与系统的转述。在无足够来源时，需要明确说明检索证据不足。

验收问题集应分别检查检索是否找到相关广告、过滤条件是否生效、引用是否支撑回答、跨数据集来源是否混淆。旧 CLAIMS 分类验证不能替代这些测试。

### 6 展示与部署范围需要明确

FA25 handoff 记载当时数据保持内部使用；FA26 要求部署，并要求原文链接可配置开关。因此仍需明确部署对象、是否限制访问、哪些原文/截图/标签可显示，以及归档链接与访问开关的具体方案。

这反映的是材料中的待确认产品边界；本次没有对版权许可作法律结论，也没有发布任何附件。

## 五 接手顺序建议

1. 补齐可读源码、社交样本、原型和归档索引，确定各数据文件的正式版本。
2. 定义记录 ID、company/sponsor 映射、日期精度、百分比分母及共享字段/特有字段。
3. 先完成基于可靠统计的两视图看板，再将可追溯的 RAG 检索接入同一筛选上下文。
4. 用独立的问题集验证检索、回答与引用，补齐部署和交接。

本学期工作的主要难点是数据衔接、统计口径、可追溯检索和产品交付。CLAIMS 的进一步研发可以保留为后续工作。

## 核查底稿

- [数据审查与具体文件位置](<C:/Users/yaobc/Documents/ChatGPT/549 native ads/analysis/data_audit.md>)
- [旧方法和验证审查](<C:/Users/yaobc/Documents/ChatGPT/549 native ads/analysis/prior_methods_review.md>)
- [从已有预测重算的分类指标](<C:/Users/yaobc/Documents/ChatGPT/549 native ads/analysis/prior_validation_recomputed.json>)
