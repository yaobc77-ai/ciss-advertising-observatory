# PDF-265：有限正文续文与来源定位

日期：2026-09-16。版本 0.2.3。AI 辅助数据审核，不是人工语义验收或广告事实核查。

## 恢复范围

目标是 CNBC 的 **Using mollusks to monitor industrial sites**，record_id 为 `d340f887-efa7-5746-aaf8-14aabba6b63f`。旧清洗 CSV 正文为 2,998 字符，停在 `2.5 million data...`。新的不可变 PDF 抽取文本为 7,098 字符，其中 6 个经审核区间、合计 **4,767 字符**用于检索。

新增可用内容包括每日数据采集的持续时间、极冷与极热环境的应用，以及河口环境仍待验证的限定；也保留结尾“其他工业场地计划部署”的表述。本文只是该广告的文字记录，没有证明其可靠性、成本或环境绩效。

**这不是完整全文恢复。** 六页 PDF 已逐页渲染核对：固定导航遮住部分页首／页尾文字，部分超链接在白色背景中不可见，第 5 页的信息图没有进入文字提取。原页面本轮未能在线读取。不能据此回答全文中没有什么，也不能用未转录的信息图数字回答。

## 为什么可以关联到这条记录

[独立来源审核](pdf265_identity_review.md)核对了原采集 ZIP 中的真实 PDF 条目、同包 URL 清单及多处独特正文锚点。PDF 字节哈希相同，原文件名与清单的 publisher/date/title 规则唯一对应。此关联强于标题匹配；确认范围是**本地采集来源与文章内容**，不是当前线上 canonical 页面或发表时间的独立认证。

原索引保留其生成时的候选状态，新的恢复审核作为独立来源记录加入，避免重写历史结论。PDF 捕获时间也没有用于替代文章日期。

## 可检查的区间

坐标为 [抽取文本](../sources/recovered_native/PDF-265.pypdf-6.10.0.txt) 中的 Python Unicode 字符半开区间。每个区间独立分块；不是 PDF 字节坐标，也不会把被删除的导航两侧拼成一条连续引文。

| 页 | 抽取文本区间 | 保留内容 |
|---|---|---|
| 1 | `[0, 823)` | 导语、贝类与环境变化 |
| 2 | `[929, 2099)` | 监测方法、说话人和研究背景 |
| 3 | `[2339, 3557)` | 测量原理、每天数据、持续时间和自动化目标 |
| 4 | `[3737, 5012)` | 环境评估、冷热海域应用、河口仍待验证 |
| 5 | `[5429, 5663)` | 其他场地的部署计划和结语 |
| 5 | `[5733, 5780)` | turbidity 的原脚注释义 |

跨页的 `Our aim` 引语及成本比较句不在这些区间内；仍完整保存在原始抽取文件中供后续审核。相关故事链接、分享提示、付费披露和站点页脚也保留在抽取文件，但不进入正文检索。披露仍使用已有元数据字段，未用 PDF 文字擅自覆盖。

## 可复现的工程处理

- [恢复 manifest](../config/native_body_recoveries.json)固定旧 CSV 哈希、源行、记录 ID／URL、旧正文哈希、PDF 哈希、抽取文件哈希、页码映射及区间。
- [提取脚本](../scripts/extract_pdf_text.py)复用 `pypdf 6.10.0`，按页保留原始文字与换行，每页追加一个 `U+000C` 分页符，不改字、不 OCR、不重写 PDF。维护环境可安装 `uv sync --extra test --extra pdf`；运行观测站本身不需要 PDF 提取器。
- 在旧 CSV 区间审核之后应用恢复；所有文件、身份和区间先验证，成功后才改内存 batch。正式导入要求 manifest 存在，避免配置丢失后悄悄回退到旧正文。
- 数据库继续使用既有版本与 chunk 机制。新正文使用新坐标；旧版本与旧引用保存。该记录旧 CLAIMS 标签移到内部 `previous_body_annotations`，当前正文不继承未经重标的标签过滤。
- 其他记录、原始 CSV/PDF、元数据和计数资格不变。`body_partial_recovery` 保存具体限制；原 CSV 截断历史也继续保留。

提取命令（输出文件必须不存在）：

```powershell
uv run --extra pdf python scripts/extract_pdf_text.py `
  'sources/pdf_archive_20260915/pdfs/summer_2025_run/CNBC/2018-12-28T10_21_52-0500_Usingmolluskstomonitorindustrialsites.pdf' `
  --expected-sha256 20b2ec33f808d96dc749ce0c62cd8b35dec43da48d7df0db44905544cb93479c `
  --out 'sources/recovered_native/PDF-265.pypdf-6.10.0.txt'
```

| 哈希对象 | SHA-256 |
|---|---|
| 原 PDF | `20b2ec33f808d96dc749ce0c62cd8b35dec43da48d7df0db44905544cb93479c` |
| 新抽取文本 | `28110347b8b96cd32ac4b7bcac41d1f3b80c67798e220f9043382eb40a622b67` |
| 旧正文 | `fdd9c9ffd4d1934e15e74c3be0a3bf687ccb7a1989b109e226dba4bc28e35ff7` |

## 验证状态

已发布到本机数据库，见 [入库与定位验证](../outputs/pdf265_publication_validation_20260916.json)、[首次导入](../outputs/native_import_pdf265_20260916.json) 与 [重复导入](../outputs/native_import_pdf265_repeat_20260916.json)。

| 检查 | 实际结果 |
|---|---|
| 当前数据版本 | `5114ebc1cf9afe59cdaa715e3ea45166` |
| 目标新正文版本 | `4a3327449b00061b4be47ef667a3a106d284af36ebd4f88685942bcc60ccf949` |
| 版本变化 | 1 条新版本、274 条不变、0 条停用 |
| 再次导入 | 275 条不变、0 新版本、0 停用 |
| 数据口径 | 275 收录、263 可计数、226 可检索、558 当前片段 |
| 定位 | 558 个当前片段逐字切片一致；86 个历史证据仍有效 |
| 原开发题支持原句 | 15 段仍可定位；没有重跑原 20 题付费评估 |
| 原始材料与元数据 | 原 CSV/PDF 哈希、目标元数据、计数资格和其他 274 条记录版本均未变 |
| 旧标签 | 原始 2 组注释与旧正文哈希保留；不用于新正文的当前标签过滤 |
| 工程测试 | 全套 `python -m pytest -q`：209 passed / 19.63 秒；Ruff 通过 |
| 安装与服务 | 0.2.3 wheel 独立安装；健康、页面布局、CSS 均 HTTP 200；本机服务重启后为新数据版本 |

入库前生成了时间戳备份 `.runtime/backups/observatory-20260916T193430104629Z.dump`。这只是更新前备份，本轮没有把它说成新数据的恢复演练。

## 两道定向付费冒烟题

复用已有评测器，新增独立的 [smoke 题文件](../eval/pdf265_recovery_smoke.jsonl)，在调用前固定问题、所需原句和说明。两题都限定到该文章，验证恢复后的“持续时间”和“河口待验证”内容能进入问答；它们不替代项目原有的 20 开发题和 20 验收草案，也不衡量跨文章检索泛化。

[保存输出](../outputs/pdf265_recovery_paid_smoke_20260916.json)：run `6c250056841b47feab178ffcc95bf2bd`，绑定本次 `5114…` 数据版本。两题回答、支持片段 2/2、返回证据定位 6/6、引文定位 2/2，语言启发式检查 2 match；人工语义支持率未测量，`overall_pass=null`。这组没有计数／无证据题，不能从零分母推断它们通过。

保存输出 SHA-256：`6c74b93fe093c9491e729cfefda23a2d7d94d11369de466e775b351dd617548e`；题文件 SHA-256：`ffeb40fc421196b233f7e3a1178831dfff48574f7ffc99c0be3d8f2464d7ee18`。运行记录中的 Python 模块哈希已与本次源文件核对一致。

- 第一答保留每天、至少两三年和 `can`，对应原引文中的完整中间句。所选引文仍带前后不完整上下文，阅读体验有待改进；不能把这段引文整体描述为一个完整句子。
- 第二答保留河口仍待验证，但写成 `Laurent Cazes says the advertisement has shown…`，将实施／展示技术的主体写成了广告。原文是受访者说 `We've shown…`。核心限定可定位，主体表达仍不够准确，不能标为语义验收通过。

以上为本轮 AI 阅读意见，人工判定与人工备注仍为空：

| 条目 | 人工判定 | 人工备注 |
|---|---|---|
| pdf265-smoke-01 | | |
| pdf265-smoke-02 | | |

6 个新片段 embedding 花费 $0.00002284，两题问答含查询 embedding 为 $0.00118708，**本轮合计 $0.00120992**。应用九月账本此后为 133 条调用、$0.13731723、未决调用 0；不包含账户其他项目。

0.2.2 的付费开发结果、演示稿及讲稿继续绑定旧 `d85…` 快照（554 块）；本次只新增正文恢复和定向检查，不改写旧结果。社交数据、固定公网、指定 GitHub、独立人工语义验收仍待完成。
