# 0.2.0：新增记录接入与正文区间修复

日期：2026-09-16。当前数据版本 `d85a98002e4493f0376c260ad82253ee`。原生广告应用已更新；完整 M1–M8 仍未验收完成。

## 已落实的数据变化

| 项目 | 0.1.1 快照 | 当前 |
|---|---:|---:|
| 收录记录 | 268 | 275 |
| 可统计记录 | 256 | 263 |
| 可检索正文 | 221 | 226 |
| 当前文本块 | 510 | 554 |
| 未知/冲突日期（可统计集合） | 20 | 22 |

- [12 项审核](additional_url_review.md)已经通过 [纳入清单](../config/native_admissions.json)应用：7 纳入、4 排除、1 待核。7 项中 5 项允许正文检索，2 项仅元数据；CERAWeek 按既有行业付费内容范围纳入，赞助方仍未知。两项冲突日期保持未知。
- [20 项正文边界审阅](prefix_boundary_review.md)和 [区间清单](../config/native_body_ranges.json)已应用。26 个导航块共 1,979 字符被排除；保留区间总计 60,410 字符。恢复了旧边界后 27,624 字符，同时移除旧前缀中的 66 字符横幅。范围恢复不证明原始文章完整或广告主张真实。
- sponsor 沿用基准表的小写键；`WSJ` 明确简称统一为 `The Wall Street Journal`。keyword 保留原来源拼写，与基准 collection keyword 的字面规则一致。原始字段均保留，不做公司别名、母子公司或关键词到赞助方的推断。

两个清单均记录 **AI 工程审阅**，没有填成人工批准。源文件 SHA-256、URL、逻辑行和正文哈希必须匹配；区间需有序、非空、不重叠且在正文范围内。清单缺失或失效会阻止 CLI 导入，不会静默退回旧 268 条快照。原始 CSV、PDF、JSONL 未被改写。

## 输入、处理和输出

`268 基准 + 合并表候选 + 两份审阅清单 → 校验与字段处理 → 275 条版本记录 → 按原文保留区间分别切块 → 554 个块 → 缓存 embedding → Dashboard / 检索 / 引用回答`

`retrieval_ranges` 优先于旧 `retrieval_end`。每个区间独立切块，字符位置和段落编号仍对应完整原文；不能把删除间隙两侧拼起来冒充连续引文。正文区间修复也不能越过显式 `metadata_only` 禁止检索决定。94 个原有疑似截断仍保留警告，没有声称已恢复全部网页正文。

新增字段使第一次发布产生 275 个新版本，旧版本保留。之后对媒体/关键词的修订另行生成版本；[最终重复导入](../outputs/native_import_v0_2_published_repeat_20260916.json)为 **275 unchanged / 0 new_versions / 0 deactivated**。该次输入与规则可重复执行。

## 当前版本的验证

[完整验证记录](../outputs/source_revision_v0_2_20260916.json)绑定当前 `d85a…` 数据版本：

- 275 条正文仍与源 CSV 单元格逐字一致。
- 554 个当前块全部满足原文切片一致，且位于一个保留区间内。
- 0.1.1 保存的 79 个历史证据定位仍有效。
- 未改动的开发题库、验收草案分别有 15 / 17 段 gold 原句通过范围与归属检查；四道计数题仍一致。没有执行验收草案的检索或生成。
- 当前浏览器显示 263 可统计、226 可检索、22 未知日期；Southern Company 的 5 条记录合并在一个 sponsor 筛选项下。媒体仍为 8 个，不因 WSJ 简称产生额外媒体组。
- 全套工程测试 **138 passed**；最后的媒体/关键词处理另通过 9 项导入回归，Ruff 通过。工程测试使用专用 `obs_test` 和模拟 SDK，不算模型质量成绩。

[当前免费检索诊断](../outputs/development_v0_2_current_lexical_20260916.json)只执行词项检索和完整筛选计数，无生成调用。报告中的每个分母以该 JSON 为准。

## 付费诊断：定位通过，语言遵循未通过

[最新保存的付费运行](../outputs/development_reviewed_paid_final_20260916.json)为 `532453a7c43740ecbe5f4954d3522f41`，绑定 **此前的 `c5ad20ac2938e619e11fe1f8bfc26b97` 数据版本**。其后又做了媒体简称和关键词拼写修订；没有把旧运行回填为当前版本的生成成绩。

该次 13 个原生开发题：记录 Hit@5 8/8、支持片段 13/13、计数 2/2、证据定位 85/85、引用定位 14/14、无证据拒答 3/3。时间 49.549 秒、结算 $0.00220026，未决预留为 0。缓存会影响费用，不能作为任意新问题的固定成本。

但 [AI 语义复核](assisted_semantic_review_v0_2.md)发现 **3 道英文题用西班牙语或法语回答**；另有广告归因和完整性待审项。这些失败保留在 [14 行复核表](citation_review_v0_2.csv)，人工列全空。当前语言遵循只有提示要求，缺少程序检测与失败处理，下一步应修补并重新验证；不能用重复生成中偶然语言正确的一次掩盖该问题。

该运行仍为 `draft_not_frozen`、`semantic_support=pending_human_review`、`overall_pass=null`。社交 4 题、跨库 3 题保持 pending。没有证明人工语义支持率达到 95%，也没有证明广告主张真实。

## 运行与交接

当前应用入口：[本机观测站](http://127.0.0.1:8050)。0.2.0 wheel 已在独立检查环境安装；健康、布局和 CSS 检查均返回 HTTP 200。源码包与维护命令见 [交接说明](../docs/handoff.md)。旧 ZIP、旧运行及旧评估报告保留。

当前版本的新备份为 `.runtime/backups/observatory-20260916T183925331480Z.dump`；导入前备份为 `.runtime/backups/observatory-20260916T182008985455Z.dump`。旧的恢复证明只对应当时备份，本轮没有把它改写为这些新备份已完成恢复。

本轮共对 45 个未缓存文本生成 embedding，后续元数据更新重用缓存、无新增 embedding。项目月账本读取为 $0.10411156、106 笔调用记录、不确定调用 0；不包含其他程序支出。正式公众交付仍需真实社交数据、指定 GitHub、固定公网配置、语言问题修复及独立人工验收。
